version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      # API Service URLs
      - PROJECT_SERVICE_URL=http://localhost:3002
      - PAYMENT_SERVICE_URL=http://localhost:3003
      - USER_SERVICE_URL=http://localhost:3001
      
      # Razorpay Configuration
      - RAZORPAY_KEY=rzp_test_a6CEBoBbltCvzC
      
      # Company Information
      - COMPANY_NAME=Dey Education And Research Private Limited
      - CURRENCY=INR
    volumes:
      - ./template:/usr/share/nginx/html
      - ./testingUI:/usr/share/nginx/html/testingUI
    restart: unless-stopped
